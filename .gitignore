# CHUQUIPIONDO WordPress Theme
# Node / build artifacts
node_modules/
*.log

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
.vscode/
.idea/

# Build / dependency folders (kept clean)
vendor/

# WordPress deployment artifacts (zips are delivered, not version-controlled normally,
# but we track them here as release artifacts per project requirements)
